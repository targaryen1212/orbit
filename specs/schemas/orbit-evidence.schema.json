{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://orbit.dev/schemas/orbit-evidence.schema.json",
  "title": "OrbitEvidenceChunk",
  "type": "object",
  "required": ["schemaVersion", "id", "ownerId", "itemId", "kind", "text", "sourceFields"],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "const": "orbit.evidence.v0" },
    "id": { "type": "string" },
    "ownerId": { "type": "string" },
    "itemId": { "type": "string" },
    "kind": {
      "type": "string",
      "enum": ["summary", "note", "caption", "transcript", "body", "resource", "entity", "location", "event", "custom"]
    },
    "text": { "type": "string", "minLength": 1 },
    "sourceFields": { "type": "array", "items": { "type": "string" } },
    "category": { "type": "string" },
    "tags": { "type": "array", "items": { "type": "string" } },
    "entities": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },
    "resources": { "type": "array", "items": { "$ref": "./orbit-resource.schema.json" } },
    "createdAt": { "type": "string", "format": "date-time" },
    "itemCreatedAt": { "type": "string", "format": "date-time" },
    "metadata": { "type": "object", "additionalProperties": true }
  }
}
